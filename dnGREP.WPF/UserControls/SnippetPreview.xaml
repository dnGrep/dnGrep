<UserControl x:Class="dnGREP.WPF.UserControls.SnippetPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="SourceCode" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Vertical">
        <Label Content="{Binding SnippetInfo}"></Label>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Lines}" Style="{StaticResource SourceCode}">12</TextBlock>
            <TextBlock Text="{Binding Text}" Style="{StaticResource SourceCode}">Hello world</TextBlock>
        </StackPanel>        
    </StackPanel>
</UserControl>
