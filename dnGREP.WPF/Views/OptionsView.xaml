<Window x:Class="dnGREP.WPF.OptionsForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:my="clr-namespace:dnGREP.WPF"
    Title="Options" Width="Auto" WindowStyle="ToolWindow" ResizeMode="NoResize" Height="Auto" MinHeight="260" 
    Background="{StaticResource GradientBackground}"
    Loaded="Window_Load" Closing="Window_Closing" KeyDown="Window_KeyDown"
    MinWidth="453" Icon="/dnGREP;component/nGREP.ico" WindowStartupLocation="CenterOwner"
    my:DiginesisHelpProvider.HelpKeyword="options.html" my:DiginesisHelpProvider.HelpNavigator="Topic" my:DiginesisHelpProvider.ShowHelp="True" SizeToContent="WidthAndHeight">
    <Window.Resources>
        
    </Window.Resources>
    <DockPanel Width="453">
        <TabControl Name="tbMainPanel" DockPanel.Dock="Top" TabStripPlacement="Left">
            <TabItem Name="tbGeneralTab">
                <TabItem.Header>
                    <WrapPanel Orientation="Vertical">
                        <Image Source="..\Images\preferences-system.png" Width="32" Height="32"/>
                        <TextBlock>General</TextBlock>
                    </WrapPanel>
                </TabItem.Header>
                <StackPanel>
                    <GroupBox Header="Startup options" Name="grShell">
                        <WrapPanel Orientation="Vertical">
                            <CheckBox Margin="3" Name="cbRegisterShell" Checked="cbRegisterShell_CheckedChanged" Unchecked="cbRegisterShell_CheckedChanged">Enable Windows Explorer integration</CheckBox>
                            <CheckBox Margin="3" Name="cbRegisterStartup" Checked="cbRegisterStartup_CheckedChanged" Unchecked="cbRegisterStartup_CheckedChanged">Enable startup accelerator</CheckBox>
                        </WrapPanel>
                    </GroupBox>
                    <GroupBox Header="Checking for updates">
                        <WrapPanel>
                            <CheckBox Margin="3" VerticalAlignment="Center" Name="cbCheckForUpdates" Checked="cbCheckForUpdates_CheckedChanged" Unchecked="cbCheckForUpdates_CheckedChanged">Enable automatic checking every</CheckBox>
                            <TextBox Width="30" Name="tbUpdateInterval"></TextBox>
                            <Label Margin="3" VerticalAlignment="Center">days</Label>
                        </WrapPanel>
                    </GroupBox>
                    <GroupBox Header="Editor">
                        <StackPanel>
                            <RadioButton Margin="3" Name="rbDefaultEditor" Checked="rbEditorCheckedChanged" GroupName="Editor" Unchecked="rbEditorCheckedChanged">Default file specific editor</RadioButton>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>
                                <RadioButton VerticalAlignment="Center" Grid.Column="0" Margin="3" Name="rbSpecificEditor"  Checked="rbEditorCheckedChanged" GroupName="Editor" Unchecked="rbEditorCheckedChanged">Custom editor:</RadioButton>
                                <TextBox Grid.Column="1" Name="tbEditorPath" Margin="0,0,0,3"></TextBox>
                                <Button Grid.Column="2" Margin="5,0,0,0" Name="btnBrowse" Click="btnBrowse_Click">...</Button>
                                <DockPanel Grid.Row="1" Grid.Column="1">
                                    <Label>Arguments</Label>
                                    <TextBox Name="tbEditorArgs"></TextBox>
                                </DockPanel>
                                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" TextWrapping="Wrap">(use %file and %line keywords to specify file location and line number)</TextBlock>
                            </Grid>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>
            <TabItem Name="tbUserInterfaceTab">
                <TabItem.Header>
                    <WrapPanel Orientation="Vertical">
                        <Image Source="..\Images\applications-graphics.png" Width="32" Height="32"/>
                        <TextBlock HorizontalAlignment="Center">User</TextBlock>
                        <TextBlock HorizontalAlignment="Center">Interface</TextBlock>
                    </WrapPanel>
                </TabItem.Header>
                <StackPanel HorizontalAlignment="Stretch">
                    <CheckBox Name="cbShowPath" Margin="3" Checked="cbShowPath_CheckedChanged" Unchecked="cbShowPath_CheckedChanged">Show file path in results panel</CheckBox>
                    <CheckBox Margin="3" Name="cbSearchFileNameOnly">
                        <CheckBox.Content>
                            <TextBlock TextWrapping="Wrap">Allow searching for file name pattern only when "search for" is empty</TextBlock>
                        </CheckBox.Content>
                    </CheckBox>
                    <CheckBox Margin="3" Name="cbExpandResult">
                        <CheckBox.Content>
                            <TextBlock TextWrapping="Wrap">Show results tree expanded</TextBlock>
                        </CheckBox.Content>
                    </CheckBox>
                    <CheckBox Margin="3" Name="cbClearType" Checked="cbClearType_Checked" Unchecked="cbClearType_Checked">
                        <CheckBox.Content>
                            <TextBlock TextWrapping="Wrap">Enable ClearType (smoothes the way application displays text)</TextBlock>
                        </CheckBox.Content>
                    </CheckBox>
                    <Button Height="23" Name="btnClearPreviousSearches" Width="104" HorizontalAlignment="Left" Click="btnClearPreviousSearches_Click">Clear old searches</Button>
                </StackPanel>
            </TabItem>
            <TabItem Name="tbSearchOptionsTab">
                <TabItem.Header>
                    <WrapPanel Orientation="Vertical">
                        <Image Source="..\Images\system-search.png" Width="32" Height="32"/>
                        <TextBlock HorizontalAlignment="Center">Search</TextBlock>
                        <TextBlock HorizontalAlignment="Center">Params</TextBlock>
                    </WrapPanel>
                </TabItem.Header>
                <StackPanel>
                    <GroupBox Header="Phonetic search">
                        <WrapPanel>
                            <Label>Match threshold (from 0 to 1.0)</Label>
                            <TextBox Name="tbFuzzyMatchThreshold" Width="50" TextChanged="tbFuzzyMatchThreshold_TextChanged"></TextBox>
                            <Label>0 - match everything; 1.0 - exact match</Label>
                            <Label Foreground="Red" Visibility="Hidden" Name="lblFuzzyMatchError">Error: Match threshold should be a number between 0 and 1.0</Label>
                        </WrapPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>
        </TabControl>
        <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Width="70" Margin="0,0,5,5" Click="btnSave_Click">Save</Button>
        </WrapPanel>
    </DockPanel>
</Window>
